#!/usr/bin/env python
# encoding=utf8
class EN(object):
    error_application_status_to_send_offer = "unconfirmed application can't send offer"
    error_user_type_to_claim = 'only provider can do it'
